# Copy into apps/api/.env and apps/web/.env. See each app's own .env.example
# for the full list; these are the two that must agree.
DATABASE_URL=postgresql://ironvow:ironvow@localhost:5432/ironvow
NEXT_PUBLIC_API_URL=http://localhost:4000
